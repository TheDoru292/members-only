extends layout

block content
  form(action="", method="post") 
    div 
      label(for="email") Email:
      input#email(type="email", name="email")
    div 
      label(for="password") Password: 
      input#psasword(type="password", name="password")
    button Submit 
  if messages
    p messages.error
